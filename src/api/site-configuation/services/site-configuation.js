'use strict';

/**
 * site-configuation service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::site-configuation.site-configuation');
